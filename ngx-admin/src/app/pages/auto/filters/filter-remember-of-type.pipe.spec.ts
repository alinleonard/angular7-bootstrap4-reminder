import { FilterRememberOfTypePipe } from './filter-remember-of-type.pipe';

describe('FilterRememberOfTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRememberOfTypePipe();
    expect(pipe).toBeTruthy();
  });
});
